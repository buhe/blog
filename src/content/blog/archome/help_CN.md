---
title: '帮助'
description: 'Archome help'
pubDate: 'Jul 08 2026'
heroImage: '../../../assets/image_9.jpg'
---

[English](https://blog.buhe.dev/blog/archome/help/)
# Archrome 使用帮助

Archrome 是一款灵感来自 Arc 浏览器的 Chrome 扩展，用**书签文件夹**管理浏览器「空间」，把工作、学习、生活等场景分开。

数据保存在 Chrome 原生书签中，不锁在扩展内部——即使卸载扩展，你的空间与链接依然还在。

---

## 打开侧边栏

- 点击扩展图标，或
- 快捷键：`Alt + Q`（Mac 上为 `Option + Q`）

---

## 空间（Spaces）

| 操作 | 说明 |
|------|------|
| 新建空间 | 点击底部 **+** 按钮 |
| 切换空间 | 点击底部对应空间图标 |
| 自定义图标 | 在书签栏把文件夹名改成以 emoji 开头，例如 `💼 工作` |
| 删除空间 | 右键空间 → 删除（需确认） |

**切换时会发生什么：** 当前打开的标签页会保存到当前空间并关闭，然后自动打开目标空间里的标签页。

---

## 标签页与书签

侧边栏大致分为三块：

1. **Pinned（固定）** — 来自书签栏中名为 **`pin`** 的专用文件夹。放入其中的链接会固定显示在侧边栏顶部，**不随空间切换而改变**，在所有空间中保持一致。
2. **Bookmarks（书签）** — 当前空间下的收藏链接（随空间变化）
3. **Tabs（标签页）** — 当前打开的标签页

常用操作：

- **关闭标签页**：在标签页列表中操作
- **移动到其他空间**：右键标签页 → 选择目标空间
- **保存为书签**：把标签页拖到书签区域
- **固定常用链接**：将链接放入书签栏的 **`pin`** 文件夹，即可在顶部固定显示（跨空间共享）

---

## 主题

侧边栏底部可切换 **深色 / 浅色**。偏好会保存；首次使用默认跟随系统。

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt + Q` / `Option + Q` | 打开 / 关闭侧边栏 |
| `Ctrl + Shift + L` / `Cmd + Shift + L` | 打开日志查看器（调试用） |

---

## 数据说明

- 每个**空间**对应书签栏下的一个文件夹
- 空间内的**书签**就是该文件夹里的链接
- **Pinned** 对应书签栏中的 **`pin`** 文件夹，与具体空间无关
- 各空间的**打开标签**状态由扩展通过 Chrome 存储记录，书签本身始终在浏览器里
